<h1>Editing gene <%= @gene.symbol %></h1>

<% form_for(@gene) do |f| %>
  <%= f.error_messages %>

    <%= render :partial => 'form', :locals => {:f => f} %>
    <%= f.submit 'Save' %>
<% end %>

<%= link_to 'Show', @gene %> |
<%= link_to 'Back', genes_path %>